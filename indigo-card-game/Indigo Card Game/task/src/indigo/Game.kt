package indigo

class Game {
    fun run(actionReceiver: () -> UserAction) =
        when (actionReceiver()) {
            is
        }

    private fun next(deck: Deck, action: UserAction) {

    }
}